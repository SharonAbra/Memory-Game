import Header from './Header';
import MemoryGrid from "./MemoryGrid";
import Finish from './Finish';

const Game = (props) => {
          return (
        <> 
            <Header/>
            <MemoryGrid/>
            <Finish/>
        </>
      )
    }
    export default Game;